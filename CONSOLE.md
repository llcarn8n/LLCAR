# LLCAR - Руководство по интерактивной консоли

## Обзор

Интерактивная консоль LLCAR предоставляет удобный меню-интерфейс для обработки видео и аудио файлов с получением транскрибации и дополнительных функций.

## Запуск интерактивной консоли

### Методы запуска

```bash
# Метод 1: Через main.py с флагом --interactive
python main.py --interactive
python main.py -i

# Метод 2: Через специальный лаунчер
python console.py

# Метод 3: Через Make
make console
```

## Главное меню

При запуске вы увидите главное меню с следующими опциями:

```
┌─ Main Menu ─────────────────────────────────────────────────────┐
│ 1. Process single video file                                    │
│ 2. Process single audio file                                    │
│ 3. Batch process multiple files                                 │
│ 4. View processing history                                      │
│ 5. Configure settings                                           │
│ 6. Show current configuration                                   │
│ 7. Help & Documentation                                         │
│ 8. Exit                                                          │
└──────────────────────────────────────────────────────────────────┘
```

## Основные функции

### 1. Обработка одного видео файла

Позволяет обработать один видео файл с полной настройкой параметров:

**Шаги:**
1. Выберите опцию `1` в главном меню
2. Выберите видео файл:
   - Из списка файлов в директории `./input`
   - Или введите полный путь к файлу
3. Настройте параметры обработки:
   - **Язык** (en/ru/zh) - язык аудиодорожки
   - **Количество спикеров** - оставьте пустым для автоопределения
   - **Извлечение ключевых слов** - да/нет
   - **Метод извлечения** - tfidf или textrank
   - **Количество ключевых слов** - обычно 10-20
   - **Форматы вывода** - json, txt, csv (можно выбрать несколько)
4. Подтвердите обработку
5. Дождитесь завершения

**Результат:**
- Транскрибация с временными метками
- Разметка по спикерам
- Ключевые слова (опционально)
- Выходные файлы в выбранных форматах

### 2. Обработка одного аудио файла

Аналогично обработке видео, но для аудио файлов напрямую (пропускается этап извлечения аудио).

**Поддерживаемые форматы:**
- WAV
- MP3
- FLAC
- OGG
- M4A

### 3. Пакетная обработка файлов

Обработка нескольких файлов за один раз с общими настройками:

**Шаги:**
1. Выберите опцию `3`
2. Укажите директорию с файлами (по умолчанию `./input`)
3. Просмотрите список найденных файлов
4. Подтвердите обработку всех файлов
5. Настройте общие параметры (применятся ко всем файлам)
6. Дождитесь завершения обработки всех файлов

**Особенности:**
- Обрабатываются как видео, так и аудио файлы
- Единые настройки для всех файлов
- Отчет о количестве успешных/неудачных обработок
- История сохраняется для каждого файла

### 4. Просмотр истории обработки

Просмотр всех ранее обработанных файлов в текущей сессии:

**Информация:**
- Дата и время обработки
- Тип файла (видео/аудио)
- Имя файла
- Язык обработки
- Статус (успешно/ошибка)
- Время обработки
- Список выходных файлов

**Экспорт:**
- Возможность экспортировать историю в JSON файл
- Файл сохраняется в `./output/processing_history.json`

### 5. Настройка параметров

Изменение настроек приложения:

**Доступные параметры:**
- **Язык** - язык по умолчанию (en/ru/zh)
- **Вариант модели** - какую модель использовать
- **Устройство** - auto/cuda/cpu
- **Директория вывода** - куда сохранять результаты
- **HuggingFace токен** - для диаризации спикеров

**Примечание:** После изменения настроек пайплайн будет переинициализирован при следующей обработке.

### 6. Просмотр текущей конфигурации

Отображает все текущие настройки приложения:

- 🌍 Языковые настройки
- 💻 Настройки устройства
- 📁 Настройки вывода
- 🔑 Извлечение ключевых слов
- 🔧 Постобработка
- 🎵 Аудио настройки
- 🔐 Аутентификация

### 7. Справка и документация

Встроенная справка с информацией:

- 🎯 Быстрый старт
- 📖 Поддерживаемые форматы
- 🌍 Поддерживаемые языки
- ⚙️ Опции обработки
- 💡 Советы и рекомендации
- 🔗 Ссылки на ресурсы

### 8. Выход

Безопасный выход из приложения.

## Рабочий процесс

### Типичный сценарий использования

1. **Запуск консоли:**
   ```bash
   python main.py --interactive
   ```

2. **Первоначальная настройка:**
   - Выберите опцию `5` (Configure settings)
   - Установите язык по умолчанию
   - Проверьте HuggingFace токен

3. **Обработка файла:**
   - Поместите видео/аудио в `./input`
   - Выберите опцию `1` или `2`
   - Следуйте подсказкам
   - Дождитесь результатов

4. **Просмотр результатов:**
   - Файлы сохранены в `./output`
   - История доступна через опцию `4`

### Пакетная обработка проекта

1. **Подготовка:**
   - Скопируйте все файлы в `./input`
   - Убедитесь в наличии достаточного места на диске

2. **Обработка:**
   - Выберите опцию `3` (Batch process)
   - Укажите `./input` как директорию
   - Настройте общие параметры
   - Подтвердите обработку

3. **Мониторинг:**
   - Следите за прогрессом каждого файла
   - Отмечайте неудачные обработки

4. **Анализ результатов:**
   - Проверьте итоговую статистику
   - Просмотрите историю
   - Экспортируйте отчет

## Файловый браузер

Интерактивный выбор файлов с автоматическим поиском:

**Функции:**
- Автоматический поиск файлов в `./input`
- Отображение размера файлов
- Выбор по номеру из списка
- Ручной ввод пути к файлу
- Проверка существования файла

**Поддерживаемые расширения:**

**Видео:**
- .mp4
- .avi
- .mkv
- .mov
- .webm

**Аудио:**
- .wav
- .mp3
- .flac
- .ogg
- .m4a

## Форматы вывода

### JSON
Полный отчет с метаданными, статистикой и всеми сегментами:

```json
{
  "metadata": {...},
  "statistics": {...},
  "speaker_statistics": {...},
  "keywords": [...],
  "segments": [...]
}
```

### TXT
Читаемый текстовый формат с временными метками:

```
[00:00:00 - 00:00:03] SPEAKER_00: Текст высказывания

[00:00:03 - 00:00:07] SPEAKER_01: Ответ другого спикера
```

### CSV
Табличный формат для анализа в Excel/Pandas:

```csv
speaker,start,end,text,original_text
SPEAKER_00,0.5,3.2,Очищенный текст,Оригинальный текст
```

## История обработки

### Структура записи истории

```json
{
  "timestamp": "2026-02-05T20:00:00",
  "type": "video",
  "file": "/path/to/video.mp4",
  "language": "ru",
  "status": "completed",
  "processing_time": 45.23,
  "output_files": {
    "json": "/path/to/output.json",
    "txt": "/path/to/output.txt"
  }
}
```

### Экспорт истории

История может быть экспортирована в JSON файл для:
- Аудита обработанных файлов
- Анализа производительности
- Отслеживания ошибок
- Документирования работы

## Советы и рекомендации

### Производительность

1. **Используйте GPU:**
   - Настройте `device: cuda` для ускорения
   - Убедитесь в наличии CUDA-совместимой видеокарты
   - Проверьте наличие драйверов

2. **Оптимизация памяти:**
   - Для больших файлов используйте пакетную обработку
   - Закрывайте другие приложения
   - Следите за использованием RAM

3. **Качество результатов:**
   - Указывайте количество спикеров, если известно
   - Используйте качественный аудио материал
   - Выбирайте правильную языковую модель

### Работа с файлами

1. **Организация:**
   - Храните входные файлы в `./input`
   - Результаты автоматически в `./output`
   - Используйте понятные имена файлов

2. **Форматы:**
   - Для анализа данных: CSV
   - Для чтения: TXT
   - Для программной обработки: JSON

3. **Резервное копирование:**
   - Сохраняйте важные результаты
   - Экспортируйте историю регулярно

## Устранение проблем

### "HuggingFace token not configured"

**Решение:**
1. Получите токен на https://huggingface.co/settings/tokens
2. Установите через меню (опция 5)
3. Или установите переменную окружения:
   ```bash
   export HF_TOKEN="your_token_here"
   ```

### "File not found"

**Решение:**
1. Проверьте путь к файлу
2. Убедитесь, что файл существует
3. Используйте абсолютный путь
4. Проверьте права доступа

### Низкое качество диаризации

**Решение:**
1. Укажите точное количество спикеров
2. Используйте качественный аудио
3. Убедитесь в правильности языка
4. Проверьте наличие фонового шума

### Медленная обработка

**Решение:**
1. Переключитесь на GPU (опция 5)
2. Используйте более быструю модель
3. Уменьшите разрешение видео
4. Обработайте только аудио

## Горячие клавиши и команды

- `Ctrl+C` - Прервать текущую операцию
- `Enter` - Подтвердить выбор
- Числа 1-8 - Выбор пункта меню
- `y/n` - Подтверждение действия

## Примеры использования

### Пример 1: Обработка интервью

```
1. Запустите консоль: python main.py -i
2. Выберите: 1 (Process single video file)
3. Выберите файл: interview.mp4
4. Настройки:
   - Language: en
   - Speakers: 2
   - Keywords: yes
   - Method: tfidf
   - Formats: json,txt
5. Подтвердите и дождитесь результатов
```

### Пример 2: Пакетная обработка подкастов

```
1. Поместите все MP3 файлы в ./input
2. Запустите: python console.py
3. Выберите: 3 (Batch process)
4. Директория: ./input
5. Подтвердите список файлов
6. Настройки:
   - Language: ru
   - Keywords: yes
   - Method: textrank
   - Formats: json,txt,csv
7. Дождитесь обработки всех файлов
8. Просмотрите историю (опция 4)
```

### Пример 3: Настройка и тестирование

```
1. Запустите консоль
2. Настройте параметры (опция 5):
   - Language: ru
   - Device: cuda
   - Output: ./results
3. Проверьте конфигурацию (опция 6)
4. Обработайте тестовый файл (опция 1)
5. Просмотрите результаты
```

## Интеграция с другими инструментами

### Python скрипты

Результаты JSON можно загружать в Python:

```python
import json

with open('output/video_report.json', 'r') as f:
    data = json.load(f)

# Анализ сегментов
for segment in data['segments']:
    print(f"{segment['speaker']}: {segment['text']}")
```

### Excel/Pandas

CSV файлы легко импортируются:

```python
import pandas as pd

df = pd.read_csv('output/video_segments.csv')
print(df.groupby('speaker')['text'].count())
```

### Автоматизация

Используйте для автоматической обработки:

```bash
#!/bin/bash
# Автоматическая обработка новых файлов

for file in ./input/*.mp4; do
    python main.py --video "$file" --language ru --formats json txt
done
```

## Дополнительные ресурсы

- **README.md** - Основная документация
- **QUICKSTART.md** - Быстрый старт
- **MODELS.md** - Сравнение моделей
- **examples.py** - Примеры Python кода
- **GitHub Issues** - Поддержка и багрепорты

---

**Версия:** 1.0
**Дата:** 2026-02-05
**LLCAR Team**
