# LLCAR - Video Processing Pipeline

Диагностика автомобилей через обработку видео

## 📥 Начало работы

### ⚠️ Важно: Проблемы при установке зависимостей?

Если `pip install -r requirements.txt` зависает или выдает ошибки конфликтов версий:
- 📖 См. **[INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md)** - подробное руководство по установке
- 🔧 См. **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - решение распространенных проблем

### 📋 Часто задаваемые вопросы

**Куда сохраняются файлы транскрипции?** → См. [FAQ.md](FAQ.md#куда-по-умолчанию-сохраняются-файлы-транскрипции)

**Другие вопросы?** → См. полный список в [FAQ.md](FAQ.md)

### Рекомендуемый способ: Использование из исходного кода

**Windows:**
```cmd
git clone https://github.com/llcarn8n/LLCAR.git
cd LLCAR

# Используйте поэтапную установку для избежания конфликтов
# См. INSTALLATION_GUIDE.md для деталей
pip install -r requirements-recommended.txt

python main.py --interactive
```

**Linux/macOS:**
```bash
git clone https://github.com/llcarn8n/LLCAR.git
cd LLCAR
./install.sh
```

### Альтернатива: Сборка установщика Windows

Если вы хотите создать установщик для Windows:
1. См. подробную инструкцию в [BUILD.md](BUILD.md)
2. Или используйте готовые релизы (когда будут опубликованы): [GitHub Releases](https://github.com/llcarn8n/LLCAR/releases)

**Для разработчиков:** См. [RELEASE_GUIDE.md](RELEASE_GUIDE.md) для создания релизов с установщиками.

**Подробная документация по загрузке:** [DOWNLOAD.md](DOWNLOAD.md)

**Подробная документация по сборке:** [BUILD.md](BUILD.md) | [BUILD_QUICK_REF.md](BUILD_QUICK_REF.md)

---

## Описание

LLCAR - это модульный пайплайн для обработки видео в текст с разметкой по спикерам и последующей аналитикой. Система использует современные open-source модели для транскрибации речи, диаризации спикеров и извлечения ключевых слов.

## Архитектура системы

Пайплайн состоит из следующих компонентов:

1. **Извлечение аудио из видео** (ffmpeg)
2. **Диаризация речи** → определение, кто говорит когда (pyannote.audio)
3. **Транскрибация** → преобразование речи в текст (Whisper/WhisperX)
4. **Постобработка текста**:
   - Очистка от паразитов, повторов, мата
   - Извлечение ключевых слов (TF-IDF / TextRank)
   - Извлечение именованных сущностей (опционально)
5. **Структурированный вывод** (JSON/CSV/TXT)

## Поддерживаемые модели по языкам

### 🇺🇸 Английский язык
- **WhisperX** (на базе openai/whisper-large-v3)
- Точная диаризация с word-level timestamping
- GPU-ускорение (flash-attention)

### 🇷🇺 Русский язык
- **bond005/whisper-podlodka-turbo** (рекомендуется) - оптимизирована для естественной речи
- **antony66/whisper-large-v3-russian** - минимальная WER (6.39%)
- **dvislobokov/whisper-large-v3-turbo-russian** - для быстрой обработки

### 🇨🇳 Китайский язык
- **Whisper large-v3** - универсальное решение
- **FireRedASR** - специализированная модель для мандаринского (опционально)

## 🚀 Быстрый старт

### 🖥️ Вариант 1: Графический интерфейс (GUI) - НОВОЕ! ✨

**Самый простой способ!** Запустите LLCAR через удобный графический интерфейс:

**Windows:**
```cmd
launch_gui.bat
```
Или двойным щелчком по файлу `launch_gui.bat`

**Linux/macOS:**
```bash
./launch_gui.sh
# или
python gui.py
```

📖 **Подробная документация GUI:** [GUI.md](GUI.md)

**Возможности GUI:**
- ✅ Простой выбор файлов через диалоги
- ✅ Настройка всех параметров в одном окне
- ✅ Журнал обработки в реальном времени
- ✅ Индикатор прогресса
- ✅ Не требует знания командной строки

---

### 📝 Вариант 2: Интерактивная консоль

Меню-driven интерфейс в терминале:

```bash
python main.py --interactive
# или
python console.py
```

См. подробности в [CONSOLE.md](CONSOLE.md)

---

### ⌨️ Вариант 3: Командная строка

Для опытных пользователей и автоматизации:

**Windows:**
```cmd
# Клонируйте и установите
git clone https://github.com/llcarn8n/LLCAR.git
cd LLCAR
pip install -r requirements.txt

# Создайте .env файл с токеном HuggingFace
echo HF_TOKEN=ваш_токен > .env

# Запустите
python main.py --video video.mp4 --language ru
```

**Linux/macOS:**
```bash
git clone https://github.com/llcarn8n/LLCAR.git
cd LLCAR
./install.sh
```

Подробная инструкция: [QUICKSTART.md](QUICKSTART.md)

### Вариант 2: Сборка установщика Windows

Если вы хотите собрать установщик для Windows самостоятельно:
```cmd
pip install -r requirements-build.txt
python build_exe.py
```

Подробная инструкция по сборке: [BUILD.md](BUILD.md)

### Или через Docker

```bash
docker build -t llcar .
docker run -it -e HF_TOKEN=ваш_токен -v $(pwd)/input:/app/input -v $(pwd)/output:/app/output llcar --video /app/input/video.mp4
```

### Или через Make

```bash
make install  # Установка
make test     # Тестирование
make run VIDEO=input/video.mp4  # Запуск
```

## Установка

### Требования

- Python 3.8+
- FFmpeg
- CUDA (опционально, для GPU-ускорения)
- HuggingFace токен (для pyannote.audio)

### Автоматическая установка

```bash
chmod +x install.sh
./install.sh
```

Скрипт автоматически:
- Проверит зависимости
- Создаст виртуальное окружение
- Установит все пакеты
- Скачает данные NLTK
- Запустит тесты

### Ручная установка

```bash
# Установка FFmpeg (Ubuntu/Debian)
sudo apt-get install ffmpeg

# Установка FFmpeg (macOS)
brew install ffmpeg

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка Python зависимостей
pip install -r requirements.txt

# Скачивание NLTK данных
python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords')"
```

### Получение HuggingFace токена

1. Зарегистрируйтесь на [HuggingFace](https://huggingface.co/)
2. Перейдите в [Settings → Tokens](https://huggingface.co/settings/tokens)
3. Создайте новый токен с правами `read`
4. Установите токен в переменную окружения:

```bash
export HF_TOKEN="your_token_here"
```

Или добавьте в файл `.env`:

```
HF_TOKEN=your_token_here
```

## Использование

### Интерактивный режим консоли (НОВОЕ!)

Запустите интерактивную консоль для удобной работы с приложением:

```bash
# Способ 1: Через main.py
python main.py --interactive

# Способ 2: Через отдельный лаунчер
python console.py

# Способ 3: Через Make
make console
```

Интерактивная консоль предоставляет:
- 📋 Меню-интерфейс для выбора операций
- 🗂️ Браузер файлов для выбора видео/аудио
- 📦 Пакетная обработка нескольких файлов
- 📜 Просмотр истории обработки
- ⚙️ Настройка параметров через меню
- 📚 Встроенная справка и документация

### Базовое использование

```bash
# Обработка видео файла
python main.py --video /path/to/video.mp4

# Обработка аудио файла напрямую
python main.py --audio /path/to/audio.wav
```

### Продвинутое использование

```bash
# Обработка с указанием языка и модели
python main.py --video video.mp4 --language ru --model-variant default

# Обработка с указанием количества спикеров
python main.py --video video.mp4 --num-speakers 2

# Сохранение в разных форматах
python main.py --video video.mp4 --formats json txt csv

# Использование кастомной конфигурации
python main.py --video video.mp4 --config custom_config.yaml

# Извлечение ключевых слов методом TextRank
python main.py --video video.mp4 --keyword-method textrank --top-keywords 20

# Отключение извлечения ключевых слов
python main.py --video video.mp4 --no-keywords

# Использование GPU
python main.py --video video.mp4 --device cuda

# Использование CPU
python main.py --video video.mp4 --device cpu
```

### Примеры для разных языков

```bash
# Английский (WhisperX)
python main.py --video english_interview.mp4 --language en --model-variant default

# Русский (bond005/whisper-podlodka-turbo)
python main.py --video russian_podcast.mp4 --language ru --model-variant default

# Русский (antony66/whisper-large-v3-russian - минимальная WER)
python main.py --video russian_lecture.mp4 --language ru --model-variant alternative

# Китайский
python main.py --video chinese_meeting.mp4 --language zh --model-variant default
```

## Конфигурация

Настройки можно задать в файле `config.yaml`:

```yaml
# Language settings
language: "en"  # en, ru, zh

# Model variant
model_variant: "default"

# Device
device: "auto"  # auto, cuda, cpu

# HuggingFace token
hf_token: null  # or set HF_TOKEN env variable

# Output settings
output:
  directory: "./output"
  formats:
    - "json"
    - "txt"
    - "csv"

# Diarization
diarization:
  num_speakers: null  # null for automatic detection

# Post-processing
postprocessing:
  remove_fillers: true
  remove_profanity: true

# Keywords
keywords:
  enabled: true
  method: "tfidf"  # tfidf or textrank
  top_n: 10
```

## Структура проекта

```
LLCAR/
├── src/
│   ├── __init__.py
│   ├── pipeline.py           # Главный пайплайн
│   ├── audio_extraction.py   # Извлечение аудио
│   ├── diarization.py        # Диаризация спикеров
│   ├── transcription.py      # Транскрибация
│   ├── postprocessing.py     # Постобработка текста
│   └── output.py             # Форматирование вывода
├── tests/                    # Тесты
├── models/                   # Локальные модели (создается автоматически)
├── output/                   # Выходные файлы (создается автоматически)
├── main.py                   # CLI entry point
├── config.yaml               # Конфигурация
├── requirements.txt          # Зависимости
└── README.md                 # Документация
```

## Форматы вывода

### JSON
Полный отчет с метаданными, статистикой спикеров, сегментами и ключевыми словами:

```json
{
  "metadata": {
    "video_path": "/path/to/video.mp4",
    "language": "ru",
    "processing_time": 45.23
  },
  "statistics": {
    "total_segments": 156,
    "total_duration": 300.5,
    "total_words": 2341,
    "speakers": ["SPEAKER_00", "SPEAKER_01"]
  },
  "speaker_statistics": {
    "SPEAKER_00": 180.2,
    "SPEAKER_01": 120.3
  },
  "keywords": [
    {"keyword": "машинное обучение", "score": 0.89},
    {"keyword": "нейросеть", "score": 0.76}
  ],
  "segments": [
    {
      "speaker": "SPEAKER_00",
      "start": 0.5,
      "end": 3.2,
      "text": "Добро пожаловать на лекцию",
      "original_text": "Ну вот добро пожаловать на лекцию"
    }
  ]
}
```

### CSV
Таблица сегментов для анализа в Excel/Pandas:

```csv
speaker,start,end,text,original_text
SPEAKER_00,0.5,3.2,Добро пожаловать на лекцию,Ну вот добро пожаловать на лекцию
SPEAKER_01,3.5,7.8,Спасибо за приглашение,Спасибо за приглашение
```

### TXT
Читаемый текстовый формат с временными метками:

```
[00:00:00 - 00:00:03] SPEAKER_00: Добро пожаловать на лекцию

[00:00:03 - 00:00:07] SPEAKER_01: Спасибо за приглашение
```

## Технологии и модели

### ASR (Automatic Speech Recognition)
- **Whisper** (OpenAI) - базовая модель
- **WhisperX** - расширение с точной диаризацией
- **Fine-tuned модели** для русского языка
- **Faster-Whisper** - оптимизированная версия

### Диаризация
- **pyannote.audio 3.1** - state-of-the-art диаризация
- Поддержка многоязычности
- GPU-ускорение

### NLP и постобработка
- **scikit-learn** - TF-IDF для извлечения ключевых слов
- **summa** - TextRank алгоритм
- **NLTK** - обработка естественного языка
- **regex** - очистка текста

### Аудио/Видео
- **FFmpeg** - извлечение и обработка аудио
- **ffmpeg-python** - Python binding

## Производительность

### Скорость обработки (примерная)

| Устройство | Язык | Скорость | Примечание |
|-----------|------|----------|------------|
| A100 GPU | EN | ~10x realtime | WhisperX с flash-attention |
| A100 GPU | RU | ~8x realtime | bond005/whisper-podlodka-turbo |
| RTX 3090 | EN | ~6x realtime | Whisper large-v3 |
| CPU (16 cores) | EN | ~1x realtime | Без GPU ускорения |

### Качество (Word Error Rate - WER)

| Язык | Модель | WER | Примечание |
|------|--------|-----|------------|
| EN | WhisperX | ~5-7% | На чистой речи |
| RU | antony66/whisper-large-v3-russian | 6.39% | Лучший результат |
| RU | bond005/whisper-podlodka-turbo | ~8-10% | Лучшая пунктуация |
| ZH | Whisper large-v3 | ~10-12% | Универсальная модель |

## Ограничения

1. **Диаризация**: Качество диаризации на русском/китайском может быть ниже, чем на английском
2. **GPU память**: Большие модели требуют 8GB+ VRAM
3. **Длинные видео**: Рекомендуется разбивать видео >1 часа на части
4. **Фоновый шум**: Сильный шум снижает качество транскрибации

## Решение проблем

### Ошибка "HuggingFace token required"
Убедитесь, что токен установлен:
```bash
export HF_TOKEN="your_token_here"
```

### Ошибка "CUDA out of memory"
Используйте CPU или модель меньшего размера:
```bash
python main.py --video video.mp4 --device cpu
```

### Низкое качество диаризации
Попробуйте указать количество спикеров:
```bash
python main.py --video video.mp4 --num-speakers 2
```

### Ошибка FFmpeg
Убедитесь, что FFmpeg установлен:
```bash
ffmpeg -version
```

## Лицензия

MIT License

## Авторы

LLCAR Team

## Благодарности

- OpenAI за модель Whisper
- pyannote.audio за диаризацию
- HuggingFace за инфраструктуру моделей
- Сообщество open-source за fine-tuned модели

## Roadmap

- [x] **Графический интерфейс (GUI)** ✅ РЕАЛИЗОВАНО
- [x] **Интерактивная консоль для обработки видео/аудио** ✅ РЕАЛИЗОВАНО
- [x] **Пакетная обработка множества файлов** ✅ РЕАЛИЗОВАНО
- [x] **Docker контейнер** ✅ РЕАЛИЗОВАНО
- [x] **Windows инсталлятор** ✅ РЕАЛИЗОВАНО
- [ ] Поддержка FireRedASR для китайского языка
- [ ] Интеграция с Named Entity Recognition (NER)
- [ ] Web интерфейс
- [ ] Real-time обработка потокового видео
- [ ] Поддержка дополнительных языков
- [ ] Автоматическое определение языка
