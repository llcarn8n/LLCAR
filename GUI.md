# LLCAR GUI - Руководство пользователя

## Графический интерфейс для LLCAR Video Processing Pipeline

LLCAR имеет полнофункциональный графический интерфейс (GUI) для удобной работы с приложением!

**Обновлено:** 2026-02-07

---

## 🚀 Быстрый старт

### Запуск GUI

**Windows:**
```cmd
launch_gui.bat
```
Или двойным щелчком по файлу `launch_gui.bat`

**Linux/macOS:**
```bash
./launch_gui.sh
```
Или:
```bash
python gui.py
```

---

## 📋 Интерфейс приложения

### Главное окно

GUI разделен на несколько секций:

#### 1. **Выбор файлов**
- **Video/Audio:** Нажмите "Browse..." чтобы выбрать видео или аудио файл
  - Поддерживаемые форматы видео: MP4, AVI, MOV, MKV, WMV, FLV, WebM
  - Поддерживаемые форматы аудио: WAV, MP3, FLAC, OGG, M4A
- **Output Dir:** Выберите директорию для сохранения результатов (по умолчанию `./output`)

#### 2. **Настройки (Settings)**
- **Language:** Выберите язык транскрипции
  - `en` - English (английский)
  - `ru` - Russian (русский)
  - `zh` - Chinese (китайский)

- **Device:** Выберите устройство для обработки
  - `auto` - Автоматический выбор (GPU если доступен, иначе CPU)
  - `cuda` - Использовать GPU (NVIDIA CUDA)
  - `cpu` - Использовать только CPU

- **Speakers:** Количество спикеров
  - `auto` - Автоматическое определение
  - `1`, `2`, `3`, `4`, `5` - Конкретное число спикеров

- **Extract Keywords:** Флажок для извлечения ключевых слов из текста

- **HF Token:** HuggingFace токен для speaker diarization
  - Получите токен на https://huggingface.co/settings/tokens
  - Токен также можно установить через переменную окружения `HF_TOKEN` или в файле `.env`

#### 3. **Форматы вывода (Output Formats)**
Выберите форматы выходных файлов:
- **JSON:** Полный отчет с метаданными, статистикой, ключевыми словами
- **TXT:** Читаемый текст с временными метками и спикерами
- **CSV:** Таблица сегментов для анализа в Excel/Pandas

#### 4. **Кнопки управления**
- **🎬 Process File:** Начать обработку выбранного файла
- **⏹ Stop:** Остановить текущую обработку
- **🗑️ Clear Log:** Очистить журнал обработки

#### 5. **Журнал обработки (Processing Log)**
- Отображает прогресс обработки в реальном времени
- Показывает все этапы: инициализация, извлечение аудио, диаризация, транскрипция
- Отображает результаты и пути к выходным файлам

---

## 💡 Примеры использования

### Пример 1: Обработка видео на русском языке

1. Запустите GUI: `python gui.py`
2. Нажмите **Browse...** рядом с "Video/Audio" и выберите видео файл
3. Выберите **Language:** `ru` (Russian)
4. Установите **Device:** `auto` (или `cuda` если есть GPU)
5. Оставьте **Speakers:** `auto` для автоматического определения
6. Отметьте нужные форматы вывода (JSON, TXT)
7. Убедитесь, что указан **HF Token**
8. Нажмите **🎬 Process File**

### Пример 2: Обработка аудио на английском с известным числом спикеров

1. Выберите аудио файл (например, `interview.mp3`)
2. Установите **Language:** `en`
3. Установите **Speakers:** `2` (если известно, что 2 спикера)
4. Выберите форматы: JSON + CSV
5. Нажмите **Process File**

### Пример 3: Быстрая обработка с CPU

1. Выберите видео файл
2. Установите **Device:** `cpu`
3. Установите нужный язык
4. Нажмите **Process File**

---

## ⚙️ Настройка HuggingFace токена

HuggingFace токен необходим для работы speaker diarization (определение говорящих).

### Способ 1: Через GUI
Введите токен в поле **HF Token** в интерфейсе

### Способ 2: Через файл .env
Создайте или отредактируйте файл `.env` в корне проекта:
```bash
HF_TOKEN=hf_ваш_токен_здесь
```

### Способ 3: Через переменную окружения

**Windows:**
```cmd
set HF_TOKEN=hf_ваш_токен_здесь
```

**Linux/macOS:**
```bash
export HF_TOKEN=hf_ваш_токен_здесь
```

### Получение токена:
1. Зарегистрируйтесь на https://huggingface.co/
2. Перейдите в Settings → Tokens: https://huggingface.co/settings/tokens
3. Создайте новый токен с правами `read`
4. Скопируйте токен

---

## 📁 Результаты обработки

После успешной обработки файлы будут сохранены в выбранной директории вывода (по умолчанию `./output/`):

```
output/
├── transcription_20260206_143025.json  ← Полный отчет
├── transcription_20260206_143025.csv   ← Таблица сегментов
└── transcription_20260206_143025.txt   ← Текстовый файл
```

### Формат JSON
Содержит:
- Метаданные (путь к файлу, язык, время обработки)
- Статистику (количество сегментов, длительность, слова, спикеры)
- Статистику по спикерам (время говорения каждого)
- Ключевые слова (если включено)
- Все сегменты с временными метками, спикерами и текстом

### Формат TXT
Читаемый текст с форматом:
```
[00:00:00 - 00:00:03] SPEAKER_00: Текст первого спикера

[00:00:03 - 00:00:07] SPEAKER_01: Текст второго спикера
```

### Формат CSV
Таблица для Excel/Pandas:
```csv
speaker,start,end,text,original_text
SPEAKER_00,0.5,3.2,Обработанный текст,Исходный текст
```

---

## 🛠️ Решение проблем

### GUI не запускается

**Проблема:** Ошибка при запуске `python gui.py`

**Решение:**
1. Убедитесь, что установлен Python 3.8+:
   ```bash
   python --version
   ```

2. Убедитесь, что установлены все зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Проверьте, что tkinter установлен:
   ```bash
   python -c "import tkinter; print('OK')"
   ```

   Если ошибка, установите tkinter:
   - **Ubuntu/Debian:** `sudo apt-get install python3-tk`
   - **macOS:** tkinter обычно включен в Python
   - **Windows:** tkinter включен в стандартную установку Python

### Ошибка "HuggingFace token is required"

**Решение:**
Введите HF токен в поле GUI или установите через `.env` файл или переменную окружения.

### Ошибка "CUDA out of memory"

**Решение:**
Измените **Device** на `cpu` в настройках GUI.

### Обработка очень медленная

**Причины и решения:**
- **CPU режим:** Обработка на CPU медленнее. Используйте GPU если доступен
- **Длинное видео:** Обработка длинных видео занимает время пропорционально длине
- **Первый запуск:** При первом запуске модели загружаются, это занимает время

### Не находит файл после выбора

**Решение:**
Убедитесь, что путь к файлу не содержит специальных символов. Используйте английские имена файлов.

---

## ⌨️ Альтернативные способы запуска

Помимо GUI, LLCAR можно запустить:

### 1. Интерактивная консоль
```bash
python main.py --interactive
```
Меню-driven интерфейс в терминале

### 2. Командная строка
```bash
python main.py --video video.mp4 --language ru
```
Прямая обработка через CLI

### 3. Docker
```bash
docker build -t llcar .
docker run -it llcar --video /app/input/video.mp4
```

---

## 📚 Дополнительная документация

- [README.md](README.md) - Общая документация
- [FAQ.md](FAQ.md) - Часто задаваемые вопросы
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [CONSOLE.md](CONSOLE.md) - Интерактивная консоль
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

## 🎯 Возможности GUI

### Текущие возможности:
- ✅ Выбор видео/аудио файлов через диалог
- ✅ Настройка всех параметров обработки
- ✅ Выбор языка и устройства
- ✅ Настройка количества спикеров
- ✅ Выбор форматов вывода
- ✅ Журнал обработки в реальном времени
- ✅ Индикатор прогресса
- ✅ Обработка в отдельном потоке (GUI не зависает)
- ✅ Поддержка Windows, Linux, macOS

### Планируемые улучшения:
- 🔜 Пакетная обработка нескольких файлов
- 🔜 Предпросмотр результатов
- 🔜 История обработки
- 🔜 Более детальный прогресс-бар
- 🔜 Темная тема

---

## 🤝 Обратная связь

Если вы нашли ошибку или хотите предложить улучшение GUI:
- GitHub Issues: https://github.com/llcarn8n/LLCAR/issues
- Укажите версию Python и операционную систему
- Приложите скриншот, если возможно

---

## 📄 Лицензия

LLCAR GUI распространяется под лицензией MIT, как и весь проект LLCAR.
